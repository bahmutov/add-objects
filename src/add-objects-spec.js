'use strict'

/* eslint-env mocha */
const addObjects = require('.')

describe('add-objects', () => {
  it('write this test', () => {
    console.assert(addObjects, 'should export something')
  })
})
